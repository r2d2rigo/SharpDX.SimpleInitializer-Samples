<Page
    x:Class="SimpleInitializer.Samples.W81.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:SimpleInitializer.Samples.W81"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <Grid.RowDefinitions>
            <RowDefinition Height="140" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="140" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <TextBlock HorizontalAlignment="Left" TextWrapping="Wrap" Text="SimpleInitializer.Samples.W81" VerticalAlignment="Center" Grid.Column="1" Margin="48,0,0,0" FontFamily="Segoe UI" FontSize="48"/>
        </Grid>
        
        <Grid Grid.Row="1">
            <StackPanel Margin="140, 48, 0, 0">
                <Button Content="SwapChainPanel" FontFamily="Segoe UI" FontSize="32" Click="SwapChainPanel_Click"/>
                <Button Content="SwapChainBackgroundPanel" Click="SwapChainBackgroundPanel_Click" FontFamily="Segoe UI" FontSize="32"/>
            </StackPanel>
        </Grid>
    </Grid>
</Page>
